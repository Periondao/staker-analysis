{
  "name": "hardhat-project",
  "scripts": {
    "analyse": "npx hardhat run scripts/Analysis.ts --network mainnet",
    "update": "npm run analyse && git commit -am 'update sheets' && git push origin master"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "dotenv": "^16.0.3",
    "hardhat": "^2.12.6",
    "json2csv": "^6.0.0-alpha.2"
  }
}
